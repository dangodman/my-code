/**
 * @param {number} numRows
 * @return {number[][]}
 */
var generate = function (numRows) {
  const arr = []
  if (numRows == 1){
    arr.push([1])
  }
  if(numRows == 2){
    arr.push([1,1])
  }
  if(numRows == 3){
    arr.push([1,2,1])
  }
  function getg(n){
    
    if(n===numRows) return
  }
}
// [1]  [1,1] [1,2,1]  [1,3,3,1]

// 5 i  const arr = new Array(i)  [i,]

// 出口就是 n = numRows 

